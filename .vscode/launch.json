{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach Docker",
            "type": "node",
            "request": "attach",
            "address": "localhost",
            "port": 5858,
            "restart": true,
            "sourceMaps": true,
            "localRoot": "${workspaceRoot}",
            "remoteRoot": "/app",
            "outDir": "${workspaceRoot}/dist"
        }
    ]
}